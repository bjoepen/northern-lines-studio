# Build 014 – Git Workflow

Branch: `feature/journey-places-foundation`

Commit: `feat(studio): introduce journey places foundation`

Nach erfolgreicher Validation:
```bash
git status
git add .
git commit -m "feat(studio): introduce journey places foundation"
git push -u origin feature/journey-places-foundation
```
