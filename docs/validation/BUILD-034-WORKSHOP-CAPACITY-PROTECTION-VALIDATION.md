# Build 034 Validation

Real-world reference: curated Fotografie-Workshop with all four modules visible.

PASS requires:

- `Sehen`, `Gestalten`, `Belichten`, `Unterwegs` remain fully readable;
- `Licht & Wetter` remains secondary and does not overlap the Companion;
- the Footer stays at its fixed position;
- no card extends into protected zones;
- the established overflow state remains the fallback if a future composition cannot fit;
- Fjord/Ostsee World Expression stays unchanged.
