# Build 007 – Release Notes

Northern Lines Studio 0.7.0 balances the persistent Editorial Frame.

The visible change is deliberately small: the header now feels centered, quieter and more connected to the page-focused workspace. The active Travelbook is the primary context; the Editorial World remains its design context.

The `.nls` project format remains `0.4.0`; no project migration is required.
