# Build 023 – Delivery Notes

Build 023 wird nach dem Northern-Lines-Studio-Releaseverfahren als **Full Repo** und als **Final UX / Composition Fix Drop-in gegen den bereits ausgelieferten Build 023** geliefert.

Der Fix ändert keine `.nls`-Persistenz und kein Hero-Asset-Modell. Er verfeinert ausschließlich Destination-Komposition und die sichtbare Bedienhierarchie des Hero-Bildbereichs.

Das Drop-in enthält eine `APPLY-DROPIN.md` nach dem verbindlichen Build-019-Muster.

## Final Weite Grammar Fix delivery

This package supersedes the earlier Build-023 Weite spacing patch. Use the Full Repo as the authoritative Build-023 state. The Drop-in targets the immediately previous **Build 023 Weite Spacing Fix** and contains only the consolidated Weite grammar, its consistency gate update and documentation.
